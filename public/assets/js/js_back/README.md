# js_back

Esta carpeta queda reservada para scripts puente hacia backend (adaptadores, clientes API o helpers de integracion) cuando implementes nuevas vistas.

Nota: el backend del servidor sigue viviendo en `src/` y no debe moverse a `public/`.
