# UserProfile refactorizado

Reemplaza tu archivo `UserProfile.jsx` por esta carpeta `UserProfile/`.

Importación recomendada desde otros componentes:

```jsx
import UserProfile from "./UserProfile";
```

La estructura separa componentes compartidos, tabs grandes y portales para que sea más fácil conectar cada pantalla con endpoints del backend.
